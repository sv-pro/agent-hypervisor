[project]
name = "agent-hypervisor"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["pyyaml"]

[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[project.scripts]
ahc = "compiler.cli:main"

[tool.setuptools.packages.find]
where = ["src"]
